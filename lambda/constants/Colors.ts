import Theme from './Theme';

export default {
  light: {
    text: Theme.primary,
    background: Theme.bg,
    tint: Theme.accent,
    tabIconDefault: Theme.muted,
    tabIconSelected: Theme.accent,
  },
  dark: {
    text: Theme.primary,
    background: Theme.bg,
    tint: Theme.accent,
    tabIconDefault: Theme.muted,
    tabIconSelected: Theme.accent,
  },
};
