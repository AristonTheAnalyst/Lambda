/**
 * Re-exports from hooks/ — implementation lives there.
 * Kept here so existing imports (@/lib/asyncGuard) continue to work without changes.
 */
export { useAsyncGuard } from '@/hooks/useAsyncGuard';
export { useUIGuard } from '@/hooks/useUIGuard';
export { navGuard } from '@/hooks/useNavGuard';
